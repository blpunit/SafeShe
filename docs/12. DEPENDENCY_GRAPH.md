# SafeShe Dependency Graphs

This document visualizes the structural and execution dependencies between various modules in the SafeShe architecture.

## 1. Top-Level Module Dependency Graph

This graph illustrates how the high-level directories and packages depend on each other within the backend.

```mermaid
graph TD
    API[app/api] --> Services[app/services]
    API --> Schemas[app/schemas]
    
    Services --> Repositories[app/repositories]
    Services --> Models[app/models]
    Services --> Intelligence[app/intelligence]
    
    Intelligence --> Agents[app/agents]
    Intelligence --> ML[app/ml]
    
    Agents --> Tools[app/tools]
    
    Repositories --> MongoDB[(MongoDB)]
    Tools --> External[External APIs]
```

## 2. Intelligence Pipeline Dependency Graph

The Agentic AI architecture utilizes a strict, directed acyclic execution flow managed by the `JourneyIntelligenceCoordinator`.

```mermaid
graph TD
    Coordinator[JourneyIntelligenceCoordinator] --> WM[WorkflowManager]
    WM --> Registry[AgentRegistry]
    Registry --> RouterAgent[RoutingAgent]
    Registry --> WeatherAgent[WeatherAgent]
    Registry --> CommunityAgent[CommunityAgent]
    
    Coordinator --> Normalizer[DataNormalizer]
    Coordinator --> Feature[FeatureEngineer]
    
    Normalizer --> Feature
    Feature --> Predictor[DummyMLPredictor]
    
    Predictor --> Decision[DecisionEngine]
    Decision --> LLM[ReasoningTool]
    
    LLM --> Builder[ResponseBuilder]
```

## 3. Frontend Architecture Dependency Graph

How UI components map to the underlying state and API client.

```mermaid
graph TD
    App[Next.js App Router] --> Page[Page Components]
    
    Page --> UI[Shadcn UI Primitives]
    Page --> LiveMap[LiveMap / MapLibre]
    Page --> Monitor[JourneyMonitor]
    
    Monitor --> Store[Zustand useJourneyStore]
    Monitor --> Tanstack[React Query]
    
    Tanstack --> Client[Axios API Client]
    Client --> FastAPI[Backend Service]
    
    Store --> Client
```

## Circular Dependencies
*Based on the architecture review, there are currently **no known circular dependencies**.*
The backend adheres strictly to layered architecture protocols (Routers -> Services -> Intelligence -> Agents -> Providers). To enforce this, the `JourneyIntelligenceCoordinator` interacts with the Agent layer downwards, and Responses are mapped back up via Pydantic builders, ensuring the Intelligence layer never imports the API Routers or external Server contexts.
